import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Cas clients Ceres : 13 projets RevOps, CRM et IA detailles",
  description:
    "Decouvrez 13 cas clients Ceres detailles : migrations CRM, RevOps Part-Time, audit HubSpot et agents IA. Resultats concrets pour PME, scale-ups et grands groupes.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
