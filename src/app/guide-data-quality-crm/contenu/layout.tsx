import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Guide Data Quality CRM : contenu complet des 10 chapitres",
  description: "Le guide data quality CRM le plus complet. Audit 5 dimensions, deduplication, enrichissement Clay + Lemlist AI + Claude Code, maintenance automatisee.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
