import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Comment utiliser l'IA dans votre processus commercial B2B",
  description:
    "Guide complet de l'IA dans la vente B2B : 8 cas d'usage concrets (scoring, emails, resume de calls, prevision de revenus, coaching). Stack IA 2026 et ROI mesurable.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
