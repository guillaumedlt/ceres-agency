import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Inbound marketing B2B et RevOps : aligner contenu et revenue",
  description:
    "Guide complet inbound marketing B2B en 2026 : les 4 etapes du funnel, attribution, scoring, SLA, content strategy, outils HubSpot et mesure du ROI inbound dans une logique RevOps.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
